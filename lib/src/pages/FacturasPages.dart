import 'package:flutter/material.dart';

class FacturasPages extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Facturas Pages"),
      ),
    );
  }
}
