import 'package:flutter/material.dart';

class PreFacturasPages extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("PreFacturas Pages"),
      ),
    );
  }
}
