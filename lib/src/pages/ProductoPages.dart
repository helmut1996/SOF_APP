import 'package:flutter/material.dart';

class ProductoPages extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Producto Pages"),
      ),
    );
  }
}
